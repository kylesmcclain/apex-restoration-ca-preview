// Tailwind is intentionally not used — the site is styled by hand-written
// global CSS in app/globals.css (ported from the original build).
const config = {
  plugins: {},
};

export default config;
